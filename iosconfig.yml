---
  - name: capture show output
    hosts: " {{ ip_address }} "
    gather_facts: no

    tasks: 

      - name: show run
        ios_command:
          commands:
          - show run
          - show version
        register: config

      - name: save output to local directory
        copy:
        hosts: 172.21.142.150
          content: "{{ config.stdout | replace('\\n', '\n') }}"
          dest: "/home/config_output.txt"
